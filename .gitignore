__pycache__/
*.egg-info/
.pytest_cache/
*.pem
dist/
build/
*.log

# internal specs/plans — never public
docs/superpowers/
*.dc.html
# local-only scratch (drafts, design prompts) — never public. This rule lives HERE, in the
# tracked .gitignore, and not only in machine-local .git/info/exclude: an exclude file is
# not cloned, so a fresh checkout elsewhere would have no protection at all.
_local/
# internal PT-BR execution control files — never public (only EN deliverables ship)
PLAN.md
STATE.md
DECISIONS.md
QUESTIONS.md
# throwaway SSH private keys used to generate the test vectors — never commit private keys
spec/test-vectors/_key/
# cloudflare wrangler cache
.wrangler/
# Rust build artifacts (Cargo.lock IS committed for the verifier binary)
impl/rust/target/
target/
# hostile-review Q&A — internal prep, not published
docs/review-simulation.md
